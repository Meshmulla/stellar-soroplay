# URL of the Soroban compilation service (see compiler-service/).
# When unset, SoroPlay falls back to a mock WASM binary for demo/offline use.
# Point this at the deployed compiler service's /compile endpoint, e.g.:
#   COMPILATION_SERVICE_URL=https://your-compiler.up.railway.app/compile
COMPILATION_SERVICE_URL=
